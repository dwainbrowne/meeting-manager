# Frontend

This folder contains the current Meeting Manager prototype.

It includes:

- the workspace interface
- the company and contact navigation
- the meeting detail view
- the action item panel
- prototype API routes
- local sample data

Run it locally from this folder:

```bash
npm install
npm run dev
```

The prototype currently uses `data/workspace.json` so people can understand the product before the production backend is connected.
