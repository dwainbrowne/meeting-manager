# Design References

This folder contains early static design references and prototype exports for Meeting Manager.

They are useful for visual comparison while the working app evolves, but the active application lives in `frontend`.
